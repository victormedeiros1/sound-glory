import Landing from '../components/Landing';
import Container from '../components/Container';

const Home: React.FC = () => {
  return (
    <Container>
      <Landing />
    </Container>
  );
};

export default Home;
